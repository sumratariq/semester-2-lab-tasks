def fib(x):
    ''' this funtion takes a range and calculates fibonacci sequence upto that range.'''
    if x==1 or x==2:
        return 1
    else:
        return fib(x-1)+fib(x-2)
fibonacci=[]
for i in range(1,21):
    result=fib(i)
    fibonacci.append(result)
print(fibonacci)
